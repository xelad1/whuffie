Package.describe({
  name: "",
  summary: "",
  version: "0.0.1",
  git: "https://github.com"
});

Package.onUse(function (api) {
  api.versionsFrom(['METEOR@1.0']);

  api.use([]);

  api.addFiles([], []);

  api.export([], []);

});