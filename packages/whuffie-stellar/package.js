Package.describe({
  name: "whuffie:whuffie-stellar",
  summary: "A Whuffie package for Stellar network interactivity",
  version: "0.0.1",
  git: "https://github.com/sunny-g/whuffie"
});

Package.onUse(function (api) {
  api.versionsFrom(['METEOR@1.0']);

  api.use([]);

  api.imply([]);

  api.addFiles([], []);

  api.export([], []);

});